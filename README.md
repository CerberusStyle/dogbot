instal dependencies ` apt install npm nodejs -y`
go to the bots directory ` cd dogbot `
install required modules ` npm install `
edit the .env file with your BOTTOKEN and LOGCHAN 
start bot with ` node . ` 
you can also set it as a systemctl service if required. (in future this will be doen for you but for now you will need to do this yourself).
